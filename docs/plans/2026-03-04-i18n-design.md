# i18n (한/영 다국어) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 이력서 시스템에 한국어/영어 다국어 지원을 추가하여 UI 라벨과 이력서 데이터 모두 한/영 전환 가능하게 하고, PDF 출력 시 선택된 언어로 출력한다.

**Architecture:** vue-i18n으로 UI 라벨을 관리하고, Resume 스키마의 텍스트 필드를 `{ ko, en }` 객체로 변환한다. 프론트엔드에서 `useLocalized()` composable로 현재 locale에 맞는 데이터를 추출하며, PDF 엔드포인트는 `?lang=` 파라미터로 언어를 선택한다.

**Tech Stack:** Vue 3, vue-i18n 10, Mongoose 8, Puppeteer (PDF), Fastify 5

---

### Task 1: Backend — Resume 스키마에 LocalizedString 지원 추가

**Files:**
- Modify: `resume-backend/src/models/Resume.js`

**Step 1: Write the failing test**

Create: `resume-backend/tests/unit/models/resume-i18n.test.js`

```javascript
import mongoose from 'mongoose';
import { MongoMemoryServer } from 'mongodb-memory-server';

let mongoServer;

beforeAll(async () => {
  mongoServer = await MongoMemoryServer.create();
  await mongoose.connect(mongoServer.getUri());
});

afterAll(async () => {
  await mongoose.disconnect();
  await mongoServer.stop();
});

describe('Resume i18n schema', () => {
  let Resume;

  beforeEach(async () => {
    // Clear model cache to reload
    delete mongoose.models.Resume;
    delete mongoose.connection.collections.resumes;
    const mod = await import('../../../src/models/Resume.js');
    Resume = mod.default;
  });

  test('should accept localized string fields with ko and en', async () => {
    const resume = new Resume({
      userId: new mongoose.Types.ObjectId(),
      profile: {
        name: { ko: '전영창', en: 'Yeongchang Jeon' },
        role: { ko: '백엔드 개발자', en: 'Backend Developer' },
        email: 'test@test.com',
        summary: { ko: '요약', en: 'Summary' },
        address: { ko: '경기도', en: 'Gyeonggi-do' },
      },
    });
    const saved = await resume.save();
    expect(saved.profile.name.ko).toBe('전영창');
    expect(saved.profile.name.en).toBe('Yeongchang Jeon');
  });

  test('should accept plain string fields for backward compatibility', async () => {
    const resume = new Resume({
      userId: new mongoose.Types.ObjectId(),
      profile: {
        name: '전영창',
        role: '개발자',
        email: 'test@test.com',
      },
    });
    const saved = await resume.save();
    expect(saved.profile.name).toBe('전영창');
  });

  test('should accept localized experience fields', async () => {
    const resume = new Resume({
      userId: new mongoose.Types.ObjectId(),
      profile: {
        name: { ko: '전영창', en: 'Yeongchang Jeon' },
        role: { ko: '개발자', en: 'Developer' },
        email: 'test@test.com',
      },
      experience: [{
        company: { ko: '삼성전자', en: 'Samsung Electronics' },
        period: '2024.01 ~ 2024.12',
        project: { ko: '대시보드 개발', en: 'Dashboard Development' },
        description: { ko: '설명', en: 'Description' },
        position: { ko: '차장', en: 'Senior Engineer' },
        techStack: ['Java', 'Spring Boot'],
      }],
    });
    const saved = await resume.save();
    expect(saved.experience[0].company.ko).toBe('삼성전자');
    expect(saved.experience[0].company.en).toBe('Samsung Electronics');
  });

  test('should accept localized education fields', async () => {
    const resume = new Resume({
      userId: new mongoose.Types.ObjectId(),
      profile: {
        name: { ko: '전영창', en: 'YC' },
        role: { ko: '개발자', en: 'Dev' },
        email: 'test@test.com',
      },
      education: [{
        school: { ko: '충남대학교', en: 'Chungnam National University' },
        major: { ko: '컴퓨터공학과', en: 'Computer Science' },
        period: '2009년 02월 졸업',
      }],
    });
    const saved = await resume.save();
    expect(saved.education[0].school.ko).toBe('충남대학교');
  });

  test('should accept localized certification fields', async () => {
    const resume = new Resume({
      userId: new mongoose.Types.ObjectId(),
      profile: {
        name: { ko: '전영창', en: 'YC' },
        role: { ko: '개발자', en: 'Dev' },
        email: 'test@test.com',
      },
      certifications: [{
        name: { ko: '정보처리기사', en: 'Engineer Information Processing' },
        date: '2011.09',
      }],
    });
    const saved = await resume.save();
    expect(saved.certifications[0].name.ko).toBe('정보처리기사');
  });

  test('should accept localized skill category', async () => {
    const resume = new Resume({
      userId: new mongoose.Types.ObjectId(),
      profile: {
        name: { ko: '전영창', en: 'YC' },
        role: { ko: '개발자', en: 'Dev' },
        email: 'test@test.com',
      },
      skills: [{
        category: { ko: '백엔드', en: 'Backend' },
        items: ['Java', 'Spring Boot'],
      }],
    });
    const saved = await resume.save();
    expect(saved.skills[0].category.ko).toBe('백엔드');
  });
});
```

**Step 2: Run test to verify it fails**

Run: `cd resume-backend && npm test -- --testPathPattern="resume-i18n" --verbose`
Expected: FAIL — schema doesn't accept objects for name, role, etc.

**Step 3: Write minimal implementation**

Modify `resume-backend/src/models/Resume.js` — change text fields from `{ type: String }` to `{ type: mongoose.Schema.Types.Mixed }` to accept both String and `{ ko, en }` objects:

```javascript
import mongoose from 'mongoose';

/**
 * Localized string — accepts both plain String and { ko, en } object
 */
const LocalizedString = { type: mongoose.Schema.Types.Mixed };
const LocalizedStringRequired = { type: mongoose.Schema.Types.Mixed, required: true };

/**
 * 프로필 스키마
 */
const profileSchema = new mongoose.Schema(
  {
    name: LocalizedStringRequired,
    role: LocalizedStringRequired,
    email: { type: String, required: true },
    phone: String,
    address: LocalizedString,
    summary: LocalizedString,
  },
  { _id: false }
);

/**
 * 스킬 카테고리 스키마
 */
const skillCategorySchema = new mongoose.Schema(
  {
    category: LocalizedStringRequired,
    items: [String],
  },
  { _id: false }
);

/**
 * 경력 스키마
 */
const experienceSchema = new mongoose.Schema({
  company: LocalizedStringRequired,
  period: { type: String, required: true },
  position: LocalizedString,
  project: LocalizedStringRequired,
  description: LocalizedString,
  techStack: [String],
});

/**
 * 학력 스키마
 */
const educationSchema = new mongoose.Schema(
  {
    school: LocalizedStringRequired,
    major: LocalizedString,
    period: String,
  },
  { _id: false }
);

/**
 * 자격증 스키마
 */
const certificationSchema = new mongoose.Schema(
  {
    name: LocalizedStringRequired,
    date: String,
  },
  { _id: false }
);

// ... rest of Resume schema unchanged
```

**Step 4: Run test to verify it passes**

Run: `cd resume-backend && npm test -- --testPathPattern="resume-i18n" --verbose`
Expected: PASS

**Step 5: Commit**

```bash
git add resume-backend/src/models/Resume.js resume-backend/tests/unit/models/resume-i18n.test.js
git commit -m "feat(backend): add LocalizedString support to Resume schema for i18n"
```

---

### Task 2: Backend — PDF 템플릿에 언어 파라미터 지원 추가

**Files:**
- Modify: `resume-backend/src/templates/resume-pdf.js`
- Modify: `resume-backend/src/controllers/resume.controller.js`

**Step 1: Write the failing test**

Create: `resume-backend/tests/unit/templates/resume-pdf-i18n.test.js`

```javascript
import { renderResumePDF } from '../../../src/templates/resume-pdf.js';

describe('renderResumePDF i18n', () => {
  const localizedResume = {
    profile: {
      name: { ko: '전영창', en: 'Yeongchang Jeon' },
      role: { ko: '백엔드 개발자', en: 'Backend Developer' },
      email: 'test@test.com',
      phone: '010-1234-5678',
      address: { ko: '경기도', en: 'Gyeonggi-do' },
      summary: { ko: '한글 요약', en: 'English summary' },
    },
    skills: [{
      category: { ko: '백엔드', en: 'Backend' },
      items: ['Java', 'Spring Boot'],
    }],
    experience: [{
      company: { ko: '삼성전자', en: 'Samsung Electronics' },
      period: '2024.01 ~ 2024.12',
      position: { ko: '차장', en: 'Senior Engineer' },
      project: { ko: '대시보드 개발', en: 'Dashboard Development' },
      description: { ko: '한글 설명', en: 'English description' },
      techStack: ['Java'],
    }],
    education: [{
      school: { ko: '충남대학교', en: 'Chungnam National University' },
      major: { ko: '컴퓨터공학과', en: 'Computer Science' },
      period: '2009',
    }],
    certifications: [{
      name: { ko: '정보처리기사', en: 'Engineer Information Processing' },
      date: '2011.09',
    }],
    personalProjects: [{
      name: 'Resume',
      description: { ko: '이력서 앱', en: 'Resume app' },
      link: 'https://github.com/test',
    }],
  };

  test('should render Korean content when lang=ko', () => {
    const html = renderResumePDF(localizedResume, 'ko');
    expect(html).toContain('전영창');
    expect(html).toContain('백엔드 개발자');
    expect(html).toContain('삼성전자');
    expect(html).toContain('경력사항');  // Korean section title
    expect(html).not.toContain('Experience</h2>');
  });

  test('should render English content when lang=en', () => {
    const html = renderResumePDF(localizedResume, 'en');
    expect(html).toContain('Yeongchang Jeon');
    expect(html).toContain('Backend Developer');
    expect(html).toContain('Samsung Electronics');
    expect(html).toContain('Experience');  // English section title
  });

  test('should default to ko when no lang provided', () => {
    const html = renderResumePDF(localizedResume);
    expect(html).toContain('전영창');
    expect(html).toContain('경력사항');
  });

  test('should fallback to ko when en value is empty', () => {
    const resume = {
      ...localizedResume,
      profile: {
        ...localizedResume.profile,
        name: { ko: '전영창', en: '' },
      },
    };
    const html = renderResumePDF(resume, 'en');
    expect(html).toContain('전영창');  // fallback to ko
  });

  test('should handle plain string fields (backward compat)', () => {
    const resume = {
      ...localizedResume,
      profile: {
        name: '전영창',
        role: '개발자',
        email: 'test@test.com',
      },
      skills: [],
      experience: [],
      education: [],
      certifications: [],
      personalProjects: [],
    };
    const html = renderResumePDF(resume, 'ko');
    expect(html).toContain('전영창');
  });
});
```

**Step 2: Run test to verify it fails**

Run: `cd resume-backend && npm test -- --testPathPattern="resume-pdf-i18n" --verbose`
Expected: FAIL — `renderResumePDF` doesn't accept `lang` parameter

**Step 3: Write implementation**

Modify `resume-backend/src/templates/resume-pdf.js`:

```javascript
/**
 * Localized field value 추출 헬퍼
 * - { ko: '...', en: '...' } 객체면 lang에 해당하는 값 반환, fallback to ko
 * - 단순 String이면 그대로 반환
 */
function L(field, lang = 'ko') {
  if (!field) return '';
  if (typeof field === 'string') return field;
  if (typeof field === 'object' && (field.ko || field.en)) {
    return field[lang] || field.ko || '';
  }
  return String(field);
}

const SECTION_TITLES = {
  ko: { skills: '스킬', experience: '경력사항', education: '학력', certifications: '자격증', projects: '개인 프로젝트' },
  en: { skills: 'Skills', experience: 'Experience', education: 'Education', certifications: 'Certifications', projects: 'Personal Projects' },
};

export function renderResumePDF(resume, lang = 'ko') {
  const { profile, skills, experience, education, certifications, personalProjects } = resume;
  const t = SECTION_TITLES[lang] || SECTION_TITLES.ko;

  // In each template section, replace direct field access with L(field, lang)
  // e.g., escapeHtml(skill.category) → escapeHtml(L(skill.category, lang))
  // e.g., escapeHtml(profile.name) → escapeHtml(L(profile.name, lang))
  // Section title: 'Skills' → t.skills
  // html lang attribute: 'ko' → lang
  // ... (full template with L() applied to all localized fields)
}
```

Apply `L(field, lang)` to every localized field in the template and use `t.skills`, `t.experience` etc. for section titles.

**Step 4: Run test to verify it passes**

Run: `cd resume-backend && npm test -- --testPathPattern="resume-pdf-i18n" --verbose`
Expected: PASS

**Step 5: Update controller to accept lang query parameter**

Modify `resume-backend/src/controllers/resume.controller.js` — `generatePDF()` function:

```javascript
export async function generatePDF(request, reply) {
  let browser;
  try {
    const lang = request.query.lang === 'en' ? 'en' : 'ko';
    // ... existing code to get resume ...

    const html = renderResumePDF(resume, lang);

    // ... existing puppeteer code ...

    // Localized filename
    const profileName = typeof resume.profile?.name === 'object'
      ? (resume.profile.name[lang] || resume.profile.name.ko || 'document')
      : (resume.profile?.name || 'document');
    const fileName = lang === 'en'
      ? `Resume_${profileName.replace(/\s+/g, '_')}.pdf`
      : `이력서_${profileName.replace(/\s+/g, '_')}.pdf`;

    // ... rest unchanged ...
  }
}
```

**Step 6: Run all backend tests**

Run: `cd resume-backend && npm test --verbose`
Expected: All tests PASS

**Step 7: Commit**

```bash
git add resume-backend/src/templates/resume-pdf.js resume-backend/src/controllers/resume.controller.js resume-backend/tests/unit/templates/resume-pdf-i18n.test.js
git commit -m "feat(backend): add i18n support to PDF template with lang parameter"
```

---

### Task 3: Frontend — vue-i18n 설치 및 locale 파일 생성

**Files:**
- Create: `resume-web/src/i18n/index.js`
- Create: `resume-web/src/i18n/locales/ko.json`
- Create: `resume-web/src/i18n/locales/en.json`
- Modify: `resume-web/src/main.js`

**Step 1: Install vue-i18n**

Run: `cd resume-web && npm install vue-i18n@10`

**Step 2: Create locale files**

Create `resume-web/src/i18n/locales/ko.json`:

```json
{
  "nav": {
    "resume": "Resume",
    "jobs": "Jobs",
    "login": "로그인",
    "logout": "로그아웃"
  },
  "profile": {
    "contactMe": "연락하기",
    "mod": "MOD",
    "experienceYears": "15y",
    "experienceLabel": "경력",
    "projectCount": "10+",
    "projectsLabel": "프로젝트",
    "emailLabel": "이메일"
  },
  "skills": {
    "title": "스킬 목록"
  },
  "experience": {
    "createPost": "새 경력 추가",
    "edit": "수정",
    "delete": "삭제",
    "copy": "복사",
    "copied": "복사됨!",
    "editTitle": "경력 수정",
    "createTitle": "경력 추가",
    "noResults": "검색 결과가 없습니다.",
    "noResultsHint": "다른 태그로 검색해 보세요.",
    "searchTag": "태그 검색",
    "count": "{n}건"
  },
  "form": {
    "company": "회사",
    "position": "직급",
    "projectName": "프로젝트명",
    "period": "기간",
    "description": "설명",
    "techStack": "기술 스택",
    "addTech": "기술 추가 (Enter)",
    "cancel": "취소",
    "saveChanges": "변경 저장",
    "create": "등록",
    "companyPlaceholder": "예: 삼성전자",
    "positionPlaceholder": "예: 시니어 개발자",
    "projectPlaceholder": "예: SCM 대시보드",
    "periodPlaceholder": "예: 2023.01 ~ 2024.12",
    "descriptionPlaceholder": "역할과 성과를 설명하세요..."
  },
  "projects": {
    "title": "개인 프로젝트"
  },
  "login": {
    "title": "로그인",
    "email": "이메일",
    "password": "비밀번호",
    "emailPlaceholder": "이메일을 입력하세요",
    "submit": "로그인",
    "submitting": "로그인 중..."
  },
  "pdf": {
    "download": "PDF 다운로드",
    "generating": "PDF 생성 중..."
  },
  "confirm": {
    "deletePost": "\"{project}\" 포스트를 삭제하시겠습니까?"
  },
  "common": {
    "loading": "이력서 데이터 로딩 중...",
    "errorTitle": "데이터 로드 실패",
    "errorFallback": "로컬 파일의 대체 데이터를 표시합니다.",
    "userAgreement": "이용약관",
    "privacyPolicy": "개인정보처리방침",
    "copyright": "© {year} {name}. All rights reserved."
  }
}
```

Create `resume-web/src/i18n/locales/en.json`:

```json
{
  "nav": {
    "resume": "Resume",
    "jobs": "Jobs",
    "login": "Log In",
    "logout": "Logout"
  },
  "profile": {
    "contactMe": "Contact Me",
    "mod": "MOD",
    "experienceYears": "15y",
    "experienceLabel": "Experience",
    "projectCount": "10+",
    "projectsLabel": "Projects",
    "emailLabel": "Email"
  },
  "skills": {
    "title": "About Skills"
  },
  "experience": {
    "createPost": "Create Post",
    "edit": "Edit",
    "delete": "Delete",
    "copy": "Copy",
    "copied": "Copied!",
    "editTitle": "Edit Experience",
    "createTitle": "Create Experience",
    "noResults": "No results found.",
    "noResultsHint": "Try a different tag.",
    "searchTag": "Search Tag",
    "count": "{n} results"
  },
  "form": {
    "company": "Company",
    "position": "Position",
    "projectName": "Project Name",
    "period": "Period",
    "description": "Description",
    "techStack": "Tech Stack",
    "addTech": "Add tech (Press Enter)",
    "cancel": "Cancel",
    "saveChanges": "Save Changes",
    "create": "Create",
    "companyPlaceholder": "e.g. Samsung Electronics",
    "positionPlaceholder": "e.g. Senior Developer",
    "projectPlaceholder": "e.g. SCM Dashboard System",
    "periodPlaceholder": "e.g. 2023.01 ~ 2024.12",
    "descriptionPlaceholder": "Describe your role and achievements..."
  },
  "projects": {
    "title": "Personal Projects"
  },
  "login": {
    "title": "Log In",
    "email": "Email",
    "password": "Password",
    "emailPlaceholder": "Enter your email",
    "submit": "Log In",
    "submitting": "Logging in..."
  },
  "pdf": {
    "download": "PDF Download",
    "generating": "Generating PDF..."
  },
  "confirm": {
    "deletePost": "Delete post \"{project}\"?"
  },
  "common": {
    "loading": "Loading resume data...",
    "errorTitle": "Failed to Load Data",
    "errorFallback": "Showing fallback data from local file.",
    "userAgreement": "User Agreement",
    "privacyPolicy": "Privacy Policy",
    "copyright": "© {year} {name}. All rights reserved."
  }
}
```

**Step 3: Create i18n instance**

Create `resume-web/src/i18n/index.js`:

```javascript
import { createI18n } from 'vue-i18n'
import ko from './locales/ko.json'
import en from './locales/en.json'

const savedLocale = localStorage.getItem('locale') || 'ko'

const i18n = createI18n({
  legacy: false,
  locale: savedLocale,
  fallbackLocale: 'ko',
  messages: { ko, en },
})

export default i18n
```

**Step 4: Register in main.js**

Modify `resume-web/src/main.js`:

```javascript
import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
import router from './router/index.js'
import i18n from './i18n/index.js'

createApp(App).use(router).use(i18n).mount('#app')
```

**Step 5: Build to verify no errors**

Run: `cd resume-web && npm run build`
Expected: Build succeeds

**Step 6: Commit**

```bash
git add resume-web/src/i18n/ resume-web/src/main.js resume-web/package.json resume-web/package-lock.json
git commit -m "feat(frontend): install vue-i18n and create ko/en locale files"
```

---

### Task 4: Frontend — useLocalized composable 생성

**Files:**
- Create: `resume-web/src/composables/useLocalized.js`

**Step 1: Create the composable**

```javascript
import { useI18n } from 'vue-i18n'

/**
 * 이력서 데이터의 localized 필드에서 현재 locale 값을 추출하는 composable
 *
 * 필드가 { ko: '...', en: '...' } 객체면 현재 locale 값을 반환
 * 단순 String이면 그대로 반환
 * 영문 값이 비어있으면 한글로 fallback
 */
export function useLocalized() {
  const { locale } = useI18n()

  /**
   * @param {string|object} field - localized field or plain string
   * @returns {string}
   */
  const localized = (field) => {
    if (!field) return ''
    if (typeof field === 'string') return field
    if (typeof field === 'object' && (field.ko !== undefined || field.en !== undefined)) {
      return field[locale.value] || field.ko || ''
    }
    return String(field)
  }

  return { localized, locale }
}
```

**Step 2: Build to verify**

Run: `cd resume-web && npm run build`
Expected: Build succeeds

**Step 3: Commit**

```bash
git add resume-web/src/composables/useLocalized.js
git commit -m "feat(frontend): add useLocalized composable for i18n data field extraction"
```

---

### Task 5: Frontend — App.vue에 언어 토글 버튼 추가

**Files:**
- Modify: `resume-web/src/App.vue`

**Step 1: Add language toggle to navbar**

In `<script setup>`, add:

```javascript
import { useI18n } from 'vue-i18n'

const { locale, t } = useI18n()

const toggleLocale = () => {
  locale.value = locale.value === 'ko' ? 'en' : 'ko'
  localStorage.setItem('locale', locale.value)
}
```

Provide locale to child components:

```javascript
provide('locale', locale)
```

In template navbar, add toggle button before the Login/Logout area (inside the right-side `<div class="flex items-center gap-4">`):

```html
<!-- Language Toggle -->
<button
  @click="toggleLocale"
  class="px-3 py-1.5 rounded-full text-sm font-bold border border-reddit-border text-reddit-text-secondary hover:text-white hover:border-white transition-colors">
  {{ locale === 'ko' ? 'EN' : 'KO' }}
</button>
```

Replace hardcoded button texts:
- `Log In` → `{{ t('nav.login') }}`
- `Logout` → `{{ t('nav.logout') }}`

**Step 2: Build and verify**

Run: `cd resume-web && npm run build`
Expected: Build succeeds

**Step 3: Commit**

```bash
git add resume-web/src/App.vue
git commit -m "feat(frontend): add language toggle button to navbar"
```

---

### Task 6: Frontend — ResumeView.vue에 i18n 적용

**Files:**
- Modify: `resume-web/src/views/ResumeView.vue`

**Step 1: Apply i18n to ResumeView**

In `<script setup>`, add:

```javascript
import { useI18n } from 'vue-i18n'
import { useLocalized } from '../composables/useLocalized.js'

const { t, locale } = useI18n()
const { localized } = useLocalized()
```

Replace hardcoded strings in template:
- `'PDF 다운로드'` → `t('pdf.download')`
- `'PDF 생성 중...'` → `t('pdf.generating')`
- `'Loading resume data...'` → `t('common.loading')`
- `'Failed to Load Data'` → `t('common.errorTitle')`
- `'Showing fallback data from local file.'` → `t('common.errorFallback')`
- `placeholder="Search Tag"` → `:placeholder="t('experience.searchTag')"`
- `{{ filteredExperiences.length }}건` → `{{ t('experience.count', { n: filteredExperiences.length }) }}`
- `User Agreement` → `{{ t('common.userAgreement') }}`
- `Privacy Policy` → `{{ t('common.privacyPolicy') }}`
- Copyright line → use `t('common.copyright', { year: new Date().getFullYear(), name: localized(data.profile.name) })`

Update delete confirm dialog:

```javascript
if (!confirm(t('confirm.deletePost', { project: localized(experience.project) }))) return
```

Update PDF download to pass locale:

```javascript
await resumeApi.downloadPDF(locale.value)
```

Pass `localized` and locale to child components via props or provide.

**Step 2: Build and verify**

Run: `cd resume-web && npm run build`
Expected: Build succeeds

**Step 3: Commit**

```bash
git add resume-web/src/views/ResumeView.vue
git commit -m "feat(frontend): apply vue-i18n to ResumeView"
```

---

### Task 7: Frontend — 모든 컴포넌트에 i18n 적용

**Files:**
- Modify: `resume-web/src/components/ProfileHeader.vue`
- Modify: `resume-web/src/components/SkillSection.vue`
- Modify: `resume-web/src/components/PersonalProjectList.vue`
- Modify: `resume-web/src/components/ExperienceItem.vue`
- Modify: `resume-web/src/components/ExperienceList.vue`
- Modify: `resume-web/src/components/ExperienceFormModal.vue`
- Modify: `resume-web/src/components/LoginModal.vue`

**Step 1: ProfileHeader.vue**

Add `useI18n` and `useLocalized`. Replace:
- `Contact Me` → `{{ t('profile.contactMe') }}`
- `MOD` → `{{ t('profile.mod') }}`
- `Experience` → `{{ t('profile.experienceLabel') }}`
- `Projects` → `{{ t('profile.projectsLabel') }}`
- `Email` → `{{ t('profile.emailLabel') }}`
- `{{ profile.name }}` → `{{ localized(profile.name) }}`
- `{{ profile.role }}` → `{{ localized(profile.role) }}`
- `{{ profile.summary }}` → `{{ localized(profile.summary) }}`

**Step 2: SkillSection.vue**

Add `useI18n` and `useLocalized`. Replace:
- `About Skills` → `{{ t('skills.title') }}`
- `{{ category.category }}` → `{{ localized(category.category) }}`

**Step 3: PersonalProjectList.vue**

Add `useI18n` and `useLocalized`. Replace:
- `Personal Projects` → `{{ t('projects.title') }}`
- `{{ project.description }}` → `{{ localized(project.description) }}`

**Step 4: ExperienceItem.vue**

Add `useI18n` and `useLocalized`. Replace:
- `Copy` / `Copied!` → `t('experience.copy')` / `t('experience.copied')`
- `Edit` → `t('experience.edit')`
- `Delete` → `t('experience.delete')`
- `{{ experience.company }}` → `{{ localized(experience.company) }}`
- `{{ experience.project }}` → `{{ localized(experience.project) }}`
- `{{ experience.position }}` → `{{ localized(experience.position) }}`
- `{{ experience.description }}` → `{{ localized(experience.description) }}`

Update `copyToClipboard` to use `localized()`.

**Step 5: ExperienceList.vue**

Add `useI18n`. Replace:
- `Create Post` → `t('experience.createPost')`
- `검색 결과가 없습니다.` → `t('experience.noResults')`
- `다른 태그로 검색해 보세요.` → `t('experience.noResultsHint')`

**Step 6: ExperienceFormModal.vue**

Add `useI18n`. Replace all form labels and placeholders with `t()` calls:
- `Edit Experience` / `Create Experience` → `t('experience.editTitle')` / `t('experience.createTitle')`
- Labels: `Company`, `Position`, etc. → `t('form.company')`, `t('form.position')`, etc.
- Placeholders → `t('form.companyPlaceholder')`, etc.
- `Cancel` → `t('form.cancel')`
- `Save Changes` / `Create` → `t('form.saveChanges')` / `t('form.create')`

**Step 7: LoginModal.vue**

Add `useI18n`. Replace:
- `Log In` (title) → `t('login.title')`
- `Email` label → `t('login.email')`
- `Password` label → `t('login.password')`
- `이메일을 입력하세요` → `t('login.emailPlaceholder')`
- `Log In` / `Logging in...` buttons → `t('login.submit')` / `t('login.submitting')`

**Step 8: Build and verify**

Run: `cd resume-web && npm run build`
Expected: Build succeeds

**Step 9: Commit**

```bash
git add resume-web/src/components/
git commit -m "feat(frontend): apply i18n to all Vue components"
```

---

### Task 8: Frontend — data.json을 다국어 형식으로 변환

**Files:**
- Modify: `resume-web/src/data.json`

**Step 1: Convert data.json**

모든 텍스트 필드를 `{ ko, en }` 형태로 변환. 기존 한글 데이터는 `ko`에, 영문 번역은 `en`에 배치.

예시 (profile 부분):

```json
{
  "profile": {
    "name": { "ko": "전영창", "en": "Yeongchang Jeon" },
    "role": { "ko": "15년+ 풀스택 개발자", "en": "15+ Years Full Stack Developer" },
    "email": "Yeongchang.jeon@gmail.com",
    "phone": "010-8838-2248",
    "address": { "ko": "경기도 화성시", "en": "Hwaseong, Gyeonggi-do" },
    "summary": { "ko": "15년 이상 Java/Spring ...", "en": "Senior full-stack developer with 15+ years of experience ..." }
  }
}
```

모든 experience, education, certifications, skills, personalProjects에 동일하게 적용.

**Step 2: Build and verify**

Run: `cd resume-web && npm run build`
Expected: Build succeeds

**Step 3: Commit**

```bash
git add resume-web/src/data.json
git commit -m "feat(frontend): convert data.json to localized format with ko/en fields"
```

---

### Task 9: Frontend — API client에 PDF lang 파라미터 추가

**Files:**
- Modify: `resume-web/src/api/client.js`

**Step 1: Update downloadPDF to accept lang parameter**

```javascript
downloadPDF: async (lang = 'ko') => {
  const token = getToken();
  const headers = {};
  if (token) {
    headers['Authorization'] = `Bearer ${token}`;
  }

  const response = await fetch(`${API_BASE}/resume/pdf?lang=${lang}`, { headers });
  // ... rest unchanged
},
```

**Step 2: Build and verify**

Run: `cd resume-web && npm run build`
Expected: Build succeeds

**Step 3: Commit**

```bash
git add resume-web/src/api/client.js
git commit -m "feat(frontend): add lang parameter to PDF download API"
```

---

### Task 10: Backend — DB 마이그레이션 스크립트 생성

**Files:**
- Create: `resume-backend/scripts/migrate-i18n.js`

**Step 1: Create migration script**

```javascript
/**
 * i18n 마이그레이션 스크립트
 * 기존 String 필드를 { ko: value, en: '' } 형태로 변환
 *
 * Usage: node scripts/migrate-i18n.js
 * Env: MONGODB_URI (required)
 */
import mongoose from 'mongoose';
import dotenv from 'dotenv';

dotenv.config();

const LOCALIZED_FIELDS = {
  profile: ['name', 'role', 'summary', 'address'],
  experience: ['company', 'position', 'project', 'description'],
  education: ['school', 'major'],
  certifications: ['name'],
  skills: ['category'],
};

function migrateField(value) {
  if (!value) return value;
  if (typeof value === 'string') return { ko: value, en: '' };
  return value; // already migrated
}

async function migrate() {
  const uri = process.env.MONGODB_URI;
  if (!uri) {
    console.error('MONGODB_URI is required');
    process.exit(1);
  }

  await mongoose.connect(uri);
  console.log('Connected to MongoDB');

  const db = mongoose.connection.db;
  const collection = db.collection('resumes');
  const resumes = await collection.find({}).toArray();
  console.log(`Found ${resumes.length} resumes to migrate`);

  for (const resume of resumes) {
    const update = {};

    // Profile fields
    for (const field of LOCALIZED_FIELDS.profile) {
      if (resume.profile?.[field] && typeof resume.profile[field] === 'string') {
        update[`profile.${field}`] = migrateField(resume.profile[field]);
      }
    }

    // Array fields
    for (const arrayName of ['experience', 'education', 'certifications', 'skills']) {
      const arr = resume[arrayName] || [];
      arr.forEach((item, i) => {
        for (const field of LOCALIZED_FIELDS[arrayName]) {
          if (item[field] && typeof item[field] === 'string') {
            update[`${arrayName}.${i}.${field}`] = migrateField(item[field]);
          }
        }
      });
    }

    if (Object.keys(update).length > 0) {
      await collection.updateOne({ _id: resume._id }, { $set: update });
      console.log(`Migrated resume ${resume._id} (${Object.keys(update).length} fields)`);
    }
  }

  console.log('Migration complete');
  await mongoose.disconnect();
}

migrate().catch(err => {
  console.error('Migration failed:', err);
  process.exit(1);
});
```

**Step 2: Commit**

```bash
git add resume-backend/scripts/migrate-i18n.js
git commit -m "feat(backend): add i18n migration script for existing resume data"
```

---

### Task 11: 기존 테스트 수정 및 전체 테스트 통과 확인

**Files:**
- Modify: existing test files as needed

**Step 1: Run all backend tests**

Run: `cd resume-backend && npm test --verbose`

Fix any failing tests due to schema changes — update test data to use `{ ko, en }` format or plain strings as appropriate.

**Step 2: Run frontend build**

Run: `cd resume-web && npm run build`

Fix any build errors.

**Step 3: Run E2E tests (if dev server available)**

Run: `cd resume-web && npm run test:e2e`

**Step 4: Commit fixes**

```bash
git add -A
git commit -m "fix: update existing tests for i18n schema compatibility"
```

---

### Task 12: 수동 검증 및 최종 커밋

**Step 1: Start dev servers**

Run (terminal 1): `cd resume-backend && npm run dev`
Run (terminal 2): `cd resume-web && npm run dev`

**Step 2: Manual verification checklist**

- [ ] 네비게이션에 KO/EN 토글 버튼이 보이는가
- [ ] 토글 클릭 시 UI 라벨이 한/영 전환되는가
- [ ] 이력서 데이터(이름, 회사명, 설명 등)가 언어에 맞게 표시되는가
- [ ] 새로고침 후에도 선택된 언어가 유지되는가
- [ ] PDF 다운로드 시 선택된 언어로 출력되는가
- [ ] 한글 PDF 파일명: `이력서_전영창.pdf`
- [ ] 영문 PDF 파일명: `Resume_Yeongchang_Jeon.pdf`
- [ ] 영문 데이터가 비어있을 때 한글로 fallback 되는가

**Step 3: Final commit if needed**

```bash
git add -A
git commit -m "feat: complete i18n (ko/en) support for resume system"
```
