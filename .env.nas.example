# ===========================================
# Synology NAS 배포용 환경 변수
# ===========================================
# 이 파일을 .env 로 복사한 뒤 값을 수정하세요:
#   cp .env.nas.example .env

# JWT 시크릿 (반드시 변경, 최소 32자)
# 생성 방법: openssl rand -hex 32
JWT_SECRET=여기에-강력한-시크릿-키를-입력하세요

# 프론트엔드 URL (CORS 허용 도메인)
# NAS IP로 접근하는 경우:
FRONTEND_URL=http://NAS_IP주소:3080
# 도메인으로 접근하는 경우:
# FRONTEND_URL=https://resume.your-domain.com

# 웹 포트 (DSM 80/443 충돌 방지)
WEB_PORT=3080

# 관리자 계정 (선택사항)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-password
